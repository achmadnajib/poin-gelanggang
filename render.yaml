services:
  - type: web
    name: poin-gelanggang
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 24.11.1
      - key: SESSION_SECRET
        generateValue: true
